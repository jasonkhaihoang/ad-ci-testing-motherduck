# E2E Incremental-Modify Retest (VD-3478)

## Changes

1. `fct_pipeline`: existing table. Comment-only test marker prepended.
   No grain, materialization, schema, or column changes.

## Models

### fct_pipeline

Materialization: table
Change: comment-only test marker (E2E CI validation, unit-tests fixture retest).
